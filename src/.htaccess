# Bloquear acceso HTTP directo a la lógica interna de controladores y vistas
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
